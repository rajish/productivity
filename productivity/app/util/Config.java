package util;

public interface Config {
//  Number of records visible in several table views
        public static final int ROW_COUNT = 20;
//      Is search of LDAP directory enabled if user not found in the database?
        public static final boolean LDAP_SEARCH = true;
}
